server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: movie-review-system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://dpshmy-nsoibg00vy6m3nxv-pub.proxy.dms.aliyuncs.com:3306/movie?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&connectTimeout=10000&socketTimeout=30000
    username: uQ0sgL2iNbfkEirFiDrsqOZo
    password: P71JlvldMkny8yqZ3DUOt0msOqB7JC
    hikari:
      initialization-fail-timeout: 60000
      connection-timeout: 30000
      minimum-idle: 1
      maximum-pool-size: 10
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-test-query: SELECT 1

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.example.movie: debug
    com.zaxxer.hikari: trace
    org.springframework.jdbc: debug

# DeepSeek 大模型配置
deepseek:
  api:
    key: ${DEEPSEEK_API_KEY:your-deepseek-api-key}
    url: https://api.deepseek.com/chat/completions
    model: deepseek-chat
